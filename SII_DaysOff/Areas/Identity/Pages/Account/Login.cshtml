@page
@model LoginModel

@inject UserManager<ApplicationUser> UserManager

@{
    //Layout = null;
    ViewData["Title"] = "IFS WebApps";
    ViewData["notShow"] = true;
}

<link href="~/css/Login.css" rel="stylesheet" />

<div class="container my-5">
    <h1 class="text-center">@ViewData["Title"]</h1>
    <div class="row my-4">
        <div class="col-md-3 container-fluid">
            <section>
                <form id="account" method="post">
                    <h5>Log in to your account</h5>
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <p class="my-2">Identifier</p>
                    <div class="form-floating">
                        <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" />
                        <label asp-for="Input.Email" class="form-label">Account Identifier</label>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
                    <p class="my-2">Password</p>
                    <div class="form-floating">
                        <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" />
                        <label asp-for="Input.Password" class="form-label"></label>
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>
                    <div class="modal fade" id="exampleModal">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h1 class="modal-title">Bienvenido!</h1>
                                    <button class="btn-close" data-bs-dismiss="modal"></button>
                                </div>
                                <div class="modal-body">
                                    @UserManager.GetUserName(User)
                                </div>
                            </div>
                        </div>
                    </div>
                    <div>
                        <button id="" type="submit" class="w-100 btn btn-lg btn-outline-primary my-3" data-bs-toggle="modal" data-bs-target="#exampleModal">
                            <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                            Log in
                        </button>
                    </div>
                    <hr class="my-3" />
                </form>
            </section>
        </div>
    </div>
</div>

<script>
    
    const myModal = document.getElementById("myAlert")

    myModal.addEventListener('show.bs.modal', event => {
        console.log('mostrado')
    })
    myModal.addEventListener('hide.bs.modal', event => {
        console.log('cerrado')
    })

    const myModalI = bootstrap.Modal.getOrCreateInstance('#exampleModal')
    myModalI.show()

</script>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
